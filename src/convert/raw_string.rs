

/// Struct declaring a matrix as its content
pub struct Matrix {
  matrix: Vec<Vec<String>>
}

/// Convert a String into a Matrix;
pub fn to_matrix(raw_string: String) {
  println!("{}", raw_string);
}